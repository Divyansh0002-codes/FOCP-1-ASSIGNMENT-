#include<stdio.h>
int main()
{
    int a,b,s,c;
    printf("Enter two number : ");
    scanf("%d%d",&a,&b);
    
        for(s=1;s<=a&&s<=b;s++)
        {
            if(a%d==0 && b%d==0)
            c=s;
        }
         printf("HCF=%d",c);
     return 0;
}
